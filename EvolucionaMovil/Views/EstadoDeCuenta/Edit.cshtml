@model EvolucionaMovil.Models.CuentaVM

@{
    ViewBag.Title = "Edit";
}

<h1>Edit</h1>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Cuenta</legend>

        @Html.HiddenFor(model => model.PayCenterId)

        @Html.HiddenFor(model => model.CuentaId)

        <div class="editor-label">
            @Html.LabelFor(model => model.TipoCuenta)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TipoCuenta)
            @Html.ValidationMessageFor(model => model.TipoCuenta)
        </div>

        <p>
            <input type="submit" class="freshbutton-blue" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
